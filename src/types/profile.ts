export interface directorDetailsType {
  title: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  designation: string;
}

export interface primaryContactType {
  title: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  designation: string;
}
